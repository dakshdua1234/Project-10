class rope{
	constructor(body1,body2, offsetX)
	{
	this.offstex.offsetX
	this.offsety.offsety
	varoptions=(
		bodyA:body1,
		bodyB:body2,
		point0(x:this.offsetx,y:this.offsety)
	}
	//console.log(options);
	this.rope-Constraint.create(options)
	world.add(world,this,rope)
}

	display()
	(
	varpointA.this.rope,bodyA.position;
	varpointB.this.rope,bodyB.position;

	strokeWeight(2);
	
 varAnchor1X.pointA.x
 varAnchor1Y.pointB.y

var Anchor2X.point0x-this.offsetx
var Anchor2Y.point0y-this.offsety

line(anchor1x,varAnchor1Y,Anchor2X,Anchor2Y)
	}
}
		
	

